# PixelDraw

这个项目是使用 AI 绘制像素图。区别于直接文生图，本项目采用强约束的 MCP 工具进行绘制。AI 将绘制指令发送给 MCP 工具，MCP 工具生成并返回像素图。

开发语言使用 Rust。

## 绘制方式

不要直接文生图。必须通过 MCP 工具逐像素绘制：

1. 先调用 `create_canvas(width, height)` 创建空白图纸（白底）。宽和高为像素数，范围 `1..=128`。再次调用会覆盖当前图纸。
2. 再调用 `draw_pixels({ pixels: [{ x, y, color }, ...] })` 填色。可多次调用，增量绘制。
3. 每次工具调用都会返回当前图纸放大 8 倍（最近邻）后的 PNG。
4. 完成后调用 `save_image({ filename?: "name.png" })` 将 8 倍 PNG 保存到项目 `output/` 目录。不填文件名则按时间戳自动命名。

坐标系：`(0, 0)` 为左上角，`x` 向右，`y` 向下。绘制指令只包含坐标和颜色。任一坐标越界或颜色非法时，整批像素都不会写入。

颜色必须使用下面的中文名（对应 MARD 拼豆色号，本阶段 24 色）：

| 颜色名 | MARD 色号 |
| --- | --- |
| 白色 | H2 |
| 浅灰 | H3 |
| 灰色 | H4 |
| 黑色 | H7 |
| 黄色 | A4 |
| 橙色 | A6 |
| 深橙 | A7 |
| 红色 | F5 |
| 亮红 | F2 |
| 粉色 | E6 |
| 肤色 | E11 |
| 浅肤 | E16 |
| 棕色 | G8 |
| 浅棕 | G13 |
| 绿色 | B5 |
| 深绿 | B8 |
| 亮绿 | B2 |
| 天蓝 | C4 |
| 蓝色 | C8 |
| 深蓝 | C12 |
| 青色 | C11 |
| 紫色 | D7 |
| 品红 | D13 |
| 暗红 | F11 |
